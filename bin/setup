#!/usr/bin/env bash

set -e;

cd $(dirname $0)/..;

npm install;

config_json_file="public/config.json";
if [ ! -e ${config_json_file} ]; then
  echo "{\"data\": [{\"event\": \"Hello World!\", \"timestamp\": 0}]}" > ${config_json_file};
fi
